# Watch Program

This repository includes a simple digital watch implemented in Python using Tkinter.

## Usage

Run the script with Python 3:

```bash
python3 watch.py
```

A window will open displaying the current time, updated every second.

